# thoughtfulai-technical-screen
Technical screen in python

sort(width, height, length, mass) function in package_sorter.py takes in width, height, length arguments as Decimals (units are in centimeters) and a mass argument as Decimal (unit is in kg)

## Run tests
Tests are located in `test_package_sorter.py`
To run:
1. Have python3 installed in terminal
2. In terminal, run `python3 -m unittest`